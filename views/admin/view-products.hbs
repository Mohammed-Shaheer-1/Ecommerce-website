<section class="" style="width: 100%; height: 100%;">
  <div class=" bg-light   ">

    <div class="row   mb-4 ms-5 w-25">
      <div style="" class="mt-3">
           <a href="/admin/add-product" class="btn btn-success  text-light ml-auto">Add product</a>
      </div>
      
    </div>
    
    <table class="ms-5 table" id="productsTable">
      <thead>
        <tr>
          {{!-- <th scope="col">No</th> --}}
          <th scope="col">Title</th>
          <th scope="col">discription</th>
          <th scope="col">price</th>
          <th scope="col">image</th>
          <th >Options</th>
        </tr>
      </thead>
      <tbody>
        {{#each products}}
        <tr>
          {{!-- <th scope="row">{{@index}}</th> --}}
          <td>{{this.name}}</td>
          <td>{{this.discription}}</td>
          <td>₹{{this.price}}</td>
          <td><img style="width: 70px; hieght: 70px;" src="/product-imges/{{this._id}}.jpg" alt=""></td>
          <td><a href="/admin/edit-product/{{this._id}}" class="btn btn-primary">Edit</a>
          <a href="/admin/delete-product/{{this._id}}" onclick="return confirm('Are you want delete {{this.name}}')" class="btn btn-danger">Delete</a></td>
        </tr> 
        {{/each}}

      </tbody>
    </table>
  </div>
</section>
{{!-- datatables --}}
<script>
  $(document).ready( function () {
    $('#productsTable').DataTable();
} );
</script>